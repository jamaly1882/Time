name: time_reminder
description: تطبيق منظم وقتي - جدول أسبوعي مع تنبيهات صوتية.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^19.4.0
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  timezone: ^0.10.1
  flutter_tts: ^4.2.3
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
